Quick example to show interaction with the ADALM Pluto SDR from Python. Note,
this required `sudo apt install libiio-utils` to be able to run. This example
doesn't show much as it's just printing the high level abstractions provided by
the Pluto specific library.
