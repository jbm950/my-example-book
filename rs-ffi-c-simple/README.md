Simple example that shows Rust calling a hello world function from C. Note this
example does not include cargo but rather has a custom build/run script. Future
C FFI examples will include a build.rs script so that compilation happens with
cargo rather than needing to do a separate gcc call.
