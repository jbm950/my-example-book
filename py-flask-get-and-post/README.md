Simple example that shows how to set up get and post endpoints using Flask.
They can either be combined into the same function or split between 2
functions.

To run the example have 2 terminals open.

First terminal: `uv run flask run`
Second terminal: `uv run runner.py`
