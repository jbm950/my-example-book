Simple example that shows logging to multiple output destinations as well as
being able to control the log level by the RUST_LOG environment variable. Note,
the environment filter requires an additional feature called out from the
tracing_subscriber crate.
