Example program that captures a self generated PWM signal to showcase the timer
capture functionality of the STM32. Note this example relies exclusively on
the PAC rather than the HAL to support learning the low level implementation
