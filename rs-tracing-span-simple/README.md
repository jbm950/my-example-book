Example showing multiple ways spans can be used from the tracing crate. Shows a
simple span, a span with context, manual child spans, instrumented functions,
nested instrumented functions and instrumented functions with custom parameter
selection/setting.
