Simple example to show sessions (taken directly from the Flask quickstart
page). Note I tried to make a client using requests but the session did not
persist between requests calls. I'd have to look more into how to persist a
session with requests calls if I wanted to go that approach.
