Project shows a simple script to sign into spotify using oauth2. At the end it
grabs current player information, pauses the active song and then resumes it to
show that the process worked. Note, if spotify is not active, these will throw
an error instead because the response will be a 404.
