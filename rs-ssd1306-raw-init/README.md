Example that turns on an SSD1306 display by sending the raw I2C commands
manually rather than relying on a library. It turns the display on, fills the
top "page" solid, waits 5 seconds and then turns the display back off.
