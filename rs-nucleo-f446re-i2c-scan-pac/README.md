Example that scans for devices on the I2C lines connected to the "Arduino
pins". For this project, I hooked up a SSD1306 display but the actual
peripheral here is not important for the example.. Note this example relies
exclusively on the PAC rather than the HAL to support learning the low level
implementation
