Example that uses the DMA peripheral to move data to be send over USART2 (which
is connected via USB to the computer).  Note this example relies exclusively on
the PAC rather than the HAL to support learning the low level implementation
