[project]
name = "py-sockets-simple"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.uv.sources]
client = { workspace = true }
server = { workspace = true }

[tool.uv.workspace]
members = ["client", "server"]
