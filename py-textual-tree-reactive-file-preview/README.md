Simple project that updates a second pane to show a preview of the file being
hovered from the tree. The difference between this and the other file preview
example is the preview is added automatically where the other one is done when
the user selects a file.
