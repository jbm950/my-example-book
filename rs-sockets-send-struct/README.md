A simple example that shows a server and client sharing a struct definition,
the struct being able to send itself to and build it self from bytes, and using
those serialize methods to send structs between the server and client.
