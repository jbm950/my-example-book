Simple button interrupt example that turns on the built in LED when the built
in button is pressed. Note this example relies exclusively on the PAC rather
than the HAL to support learning the low level implementation.
