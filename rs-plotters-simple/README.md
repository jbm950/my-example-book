Simple example showing how to use the plotters library with a quadratic
equation. Note, on Linux systems a system library may be required first. Also,
a './images' directory needs to be made or the code won't actually save the
file.
