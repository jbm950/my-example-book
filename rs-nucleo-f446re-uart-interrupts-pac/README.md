Simple USART example that showcases interrupts by echoing bytes recieved back
to the sender and sending a timer message every 2 seconds. Note this example
relies exclusively on the PAC rather than the HAL to support learning the low
level implementation
