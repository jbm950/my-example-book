Small project that has a title and body field with modal editing that can be
used to create and send issues to Gitea. The biggest remaining improvement
would be handling the field styles through state rather than manually calling
methods in `ui()` and maybe flattening the fields to an array but the current
implmentation is sufficient for the example.
