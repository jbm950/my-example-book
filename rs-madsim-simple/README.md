Simple example that shows that madsim handles the timing under the hood
independently from system time (basically patches it out). The code itself
doesn't have direct madsim calls. This example just shows how to set up the
code and dependencies to use madsim. The different call methods are shown in an
example run script.
